_sourceCategory=prod/web/access
| where endpoint = "/api/data" and response_time_ms > 3000
| count as slow_count
| where slow_count > 5

# Alert Setup:
# - Monitor Type: Logs
# - Time Range: 10 minutes
# - Trigger: When result count > 0
# - Notification: Webhook to Lambda Function URL
